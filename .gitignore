/target
node_modules/
*.node
node/dist/

# Generated wasm/py binding outputs (build artifacts; produced by wasm-pack /
# maturin before publishing).
wasm/pkg/
wasm/dist/
python/dist/
python/target/
python/.venv/
__pycache__/
*.pyc

# Generated at `npm pack` time by node/scripts/sync-docs.mjs (canonical copies
# live at the repo root as the GitHub-facing docs).
node/README.md
node/LICENSE-MIT
node/LICENSE-APACHE
node/CHANGELOG.md

# Internal-only docs (application concepts that must not go public).
docs/internal/

# Downloaded real-world data for benchmarks (Natural Earth; not part of the
# source code). Fetch with: bun run bench data
benchmarks/data/countries.geojson
benchmarks/data/deu.geojson

# Environment variables (local development only)
.env

# Throwaway local scratch/benchmark output (never part of the source).
tmp/