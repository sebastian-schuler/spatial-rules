[package]
name = "spatial-rules-benchmarks"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Benchmark harness and reference cross-checks"

[lib]
name = "spatial_rules_benchmarks"
path = "src/lib.rs"

[[bin]]
name = "generate_dataset"
path = "src/main.rs"

[dependencies]
geo.workspace = true
serde_json.workspace = true
spatial-rules-core = { path = "../core" }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "ladder"
harness = false
