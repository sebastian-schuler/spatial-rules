# Explanation: flat per-rule "why" in the resolved outcome

Type: task
Status: ready-for-agent
Blocked by: 02

## Question

Per ADR-0015 (Q9), the ordered applicable set is the explanation; each member carries the per-rule why the engine already computes. Define and emit the outcome shape:

- Per applicable rule: `{ruleId, priority, spatialMatched, propertyMatched}` — `propertyMatched` is `reaches_relate`; `spatialMatched` is the DE-9IM predicate result.
- The resolved outcome for a candidate: `{outcome: "resolved", winner, values, applicable}`; `notMatched` / `invalid { reason }` unchanged.
- No field-by-field predicate breakdown (deferred; additive later without shape change).

Tests: explanation contents for resolved / notMatched / invalid; spatial-vs-property admission distinction.

Run: `cargo test --workspace` and `cargo clippy --workspace --all-targets` — green before commit.

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Emit a flat per-rule explanation (`ruleId, priority, spatialMatched, propertyMatched`) with the resolved outcome.

**Current behavior:**
No explanation exists; match results report only rule ids.

**Desired behavior:**
- The resolved outcome for a candidate carries the ordered applicable set, each member annotated with `ruleId`, `priority`, `spatialMatched` (the query's spatial predicate held), and `propertyMatched` (the `where` clause admitted the rule).
- The winner and merged values accompany the applicable set.
- No field-by-field property predicate breakdown (deferred; additive later without changing the shape).

**Key interfaces:**
- The per-candidate resolved outcome — gains the annotated applicable set (winner + values already come from ticket 02)
- `spatialMatched`/`propertyMatched` come from data the evaluation already computes (the admission check and the DE-9IM predicate result)

**Acceptance criteria:**
- [ ] The resolved outcome carries the applicable set with per-rule `spatialMatched`/`propertyMatched`/`priority`
- [ ] `spatialMatched` and `propertyMatched` are both true for an applicable rule; a rule failing either is absent from the set
- [ ] The outcome distinguishes resolved / notMatched / invalid
- [ ] `cargo test --workspace` and `cargo clippy --workspace --all-targets` are green

**Out of scope:**
- Field-by-field property predicate traces
- Any explanation UI