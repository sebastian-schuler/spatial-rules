# Resolution test suite: property tests, determinism, precedence invariants

Type: task
Status: resolved
Blocked by: 02

## Question

turf cannot oracle resolution, so the suite is property-based + hand-checked:

- Property tests over synthetic overlapping rule sets: the winner is the max-priority applicable rule; tie-break is deterministic (declaration order); values are first-provider-wins; the result is stable across repeat evaluations and across both spatial-index kinds (RStar vs LinearScan).
- Priority-order invariants: unprioritized rules never outrank explicitly-prioritized ones; `excludeRuleIds` removes a rule from the applicable set and its fields from values.
- Edge cases: single-rule candidate, no-match, invalid candidate, all-defaults ties.

Run: `cargo test --workspace` and `cargo clippy --workspace --all-targets` — green before commit.

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Property-based and determinism tests for resolution (turf cannot oracle it).

**Current behavior:**
The suite cross-checks spatial predicates against turf; there is no oracle for resolution.

**Desired behavior:**
- Property tests over synthetic overlapping rule sets assert:
  - the winner is the max-priority applicable rule;
  - ties break deterministically by declaration order;
  - values are first-provider-wins;
  - results are stable across repeat evaluations and across both spatial-index kinds (RStar and the linear scan).
- Invariants: unprioritized rules never outrank explicitly-prioritized ones; `excludeRuleIds` removes a rule from the applicable set and its fields from the values map.
- Edge cases: single-rule candidate, no-match, invalid candidate, all-defaults ties.

**Key interfaces:**
- The resolution path (tickets 02/03) exercised through the ruleset/engine query surface, including the index-kind switchable build used by the benchmark ladder

**Acceptance criteria:**
- [ ] Property tests cover the invariants above and pass deterministically
- [ ] Resolution results agree across the RStar and linear-scan index kinds
- [ ] Edge cases have explicit cases
- [ ] `cargo test --workspace` and `cargo clippy --workspace --all-targets` are green

**Out of scope:**
- Benchmark ladder additions (separate concern)
- turf cross-checks for resolution

## Answer

Implemented in commit `96258ea` (test(core): resolution property suite — precedence invariants, determinism, index-kind parity).

- Property tests (core/tests/proptest.rs) over synthetic overlapping rule sets: applicable order is priority desc then declaration order; the winner is the head and carries the maximum applicable priority (pinned against the authoritative `Ruleset::priority`, not the implementation-filled records); values are first-provider-wins (no invented fields, no dropped defined fields); results are stable across repeat evaluations and across the RStar and LinearScan index kinds; `excludeRuleIds` removes a rule from the applicable set and its unique source field from values.
- Priorities are generated non-negative, so the "unprioritized (0) never outranks an explicitly prioritized rule" invariant is well-defined (consistent with the negative-priority gate added under ticket 01).
- Hand-checked edge cases: single-rule candidate, no-match, invalid candidate, all-defaults ties (the latter two already covered from tickets 02/03).