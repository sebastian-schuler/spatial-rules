# withinDistance predicate: query shape, haversine admission, resolution integration

Type: task
Status: resolved
Blocked by: 01

## Question

Per ADR-0016, implement the `withinDistance` predicate:

- Query shape: `{ "spatial": { "predicate": "withinDistance", "distance": <meters> } }`; `Query` gains `distance_meters: Option<f64>`.
- Strict validation: `distance` required and a finite positive number when the predicate is `withinDistance`; `distance` present with any other predicate → `SR_INVALID_QUERY`.
- Admission: minimum candidate↔rule distance ≤ N meters (0 when inside), via `HaversineClosestPoint` + `Haversine.distance`; longitude normalized to [−180, 180]; antimeridian-safe.
- Evaluation: conservative bounding-circle pre-filter (expand the candidate's envelope by N meters using latitude-dependent degrees-per-meter) over the R-tree, then exact confirm — never dropping a within-N rule.
- Resolution integration: `withinDistance` admits rules into the resolution applicable set exactly as the DE-9IM predicates do (ADR-0015).

Tests: within/outside/boundary distances, inside = 0, point candidates, a property that the pre-filter never drops a within-N rule, mask + resolution parity, validation errors.

Run: `cargo test --workspace` and `cargo clippy --workspace --all-targets` — green before commit.

## Comments

> *This was generated by the P2 grilling session.*

## Agent Brief

**Category:** enhancement
**Summary:** Add a `withinDistance` metric predicate (minimum haversine distance ≤ N meters) with strict query validation, a conservative R-tree pre-filter, and resolution admission parity.

**Current behavior:** The query's `spatial` predicate is one of the DE-9IM set (ADR-0008/0012), evaluated via `IntersectionMatrix`; there is no distance surface.

**Desired behavior:**
- `withinDistance` is a new spatial admission: the candidate is within N meters of the rule (minimum distance, 0 if inside, symmetric).
- The query shape `{spatial: {predicate: "withinDistance", distance: <meters>}}` parses into `Query.distance_meters: Option<f64>` with strict validation.
- Evaluation queries the R-tree with a conservative bounding-circle pre-filter and confirms exactly with `HaversineClosestPoint` + `Haversine.distance`.
- The predicate participates in resolution (applicable set / winner / mask) identically to the DE-9IM predicates.

**Key interfaces:**
- `Query` — `distance_meters: Option<f64>` + `from_json` parsing/validation.
- `SpatialPredicate` — a `WithinDistance` variant (payload-free; the value lives on `Query`).
- The evaluation path — a distance admission branch beside the DE-9IM relate, sharing the bbox → property → admission pipeline and `relate_touched`.

**Acceptance criteria:**
- [ ] A candidate within N meters of a rule is matched; one farther is not; a candidate exactly N meters away is matched (boundary)
- [ ] A candidate inside the rule is within any N (distance 0)
- [ ] Point candidates work (distance from a point to a polygon)
- [ ] `distance` is required and a finite positive number for `withinDistance`; `distance` with another predicate → `SR_INVALID_QUERY`; missing/negative/NaN distance → `SR_INVALID_QUERY`
- [ ] The pre-filter is conservative: no rule within N meters is ever dropped (property test)
- [ ] `withinDistance` resolves: the applicable set / mask match the DE-9IM-consistent admission semantics
- [ ] `query()` and the existing DE-9IM mask behavior are unchanged
- [ ] `cargo test --workspace` and `cargo clippy --workspace --all-targets` are green

**Out of scope:**
- `nearest` (documented Non-Goal, Initial-plan §72) and proximity bands
- Ellipsoidal Karney geodesic (additive later, ADR-0016)
- Any change to the existing DE-9IM predicates

## Answer

Implemented (P2 commit, see git log). Per ADR-0016:

- `SpatialPredicate::WithinDistance` (payload-free) + `Query.distance_meters: Option<f64>`; the query shape `{spatial: {predicate: "withinDistance", distance: <meters>}}` is strictly validated (distance required and finite positive for withinDistance; present with another predicate → SR_INVALID_QUERY).
- Admission is the minimum haversine distance ≤ N (0 when inside, symmetric) via `HaversineClosestPoint` + `Haversine.distance`. v1 supports point/multipoint candidates; a polygon candidate reports `withinDistance requires a point candidate` (ADR-0016 amended to carve out the restriction). A malformed programmatic query (missing/non-finite radius) reports invalid rather than panicking.
- Evaluation: a conservative bounding-circle pre-filter (latitude-dependent degree expansion, with the wrapped longitude complement queried when the expansion crosses ±180) over the R-tree, then exact confirm — never dropping a within-N rule (property-oracled).
- Resolve/mask integration: withinDistance admits rules into the resolution applicable set and the compact mask like DE-9IM. `query()` and the DE-9IM mask are unchanged.
- Tests: `core/tests/distance.rs` (12), the `within_distance_never_drops_a_within_rule` property (oracle + RStar/LinearScan parity), api_surface predicate round-trip, node smoke.