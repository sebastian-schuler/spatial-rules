# P2 test suite: haversine invariants, determinism, temporal edge cases

Type: task
Status: resolved
Blocked by: 02, 03

## Question

Property + hand-checked tests over the P2 surfaces:

- **withinDistance** (blocked by 03): haversine invariants (within iff distance ≤ N, inside = 0, symmetry, antimeridian wrap), the bounding-circle pre-filter never drops a within-N rule (property test over random candidate/rule pairs), determinism + index-kind parity (RStar vs LinearScan), mask + resolution parity, boundary and point-candidate edge cases.
- **Temporal** (blocked by 02): window admission, midnight wrap, empty window, missing-field non-match, day bitmask membership, `$activeAt` composition, `at` parsing/validation/required semantics, determinism.

Edge cases: exactly-N distance, point candidates, single-rule/no-match/invalid candidates, `at` with no temporal predicate, malformed `at`.

Run: `cargo test --workspace` and `cargo clippy --workspace --all-targets` — green before commit.

## Comments

> *This was generated by the P2 grilling session.*

## Agent Brief

**Category:** enhancement
**Summary:** Property-based and determinism tests for the P2 predicates (turf cannot oracle distance/time).

**Current behavior:** The suite cross-checks spatial predicates against turf and has a resolution property suite (P1); there is no distance or temporal coverage.

**Desired behavior:**
- Property tests assert withinDistance invariants (distance ≤ N ⇔ admitted, inside = 0, symmetry, antimeridian wrap) and that the pre-filter never drops a within-N rule.
- withinDistance results agree across repeat evaluations and across both spatial-index kinds.
- Temporal window edge cases have explicit hand-checked cases; `$activeAt` composition and `at` semantics are covered.
- Edge cases: exactly-N boundary, point candidates, no-match, invalid candidate, all-window edge bindings.

**Key interfaces:**
- The withinDistance admission path (ticket 03) and the `$activeAt` path (ticket 02), exercised through the ruleset/engine query surface including the index-kind switchable build.

**Acceptance criteria:**
- [ ] Property tests cover the haversine invariants and the pre-filter conservativeness, passing deterministically
- [ ] withinDistance results agree across the RStar and linear-scan index kinds
- [ ] Temporal edge cases have explicit cases (midnight wrap, empty window, missing fields, day membership)
- [ ] Edge cases (boundary distance, point candidates, no-match, invalid) have explicit cases
- [ ] `cargo test --workspace` and `cargo clippy --workspace --all-targets` are green

**Out of scope:**
- Benchmark ladder additions (separate concern)
- turf cross-checks for distance/time

## Answer

Satisfied by the tests landed with tickets 02 and 03 (the suite needed no separate ticket — all coverage shipped with the features).

- **withinDistance** (core/tests/distance.rs, 16 tests): inside=0, boundary, within/outside, point/multipoint candidates, polygon-candidate invalid, where + exclusions, strict validation, resolution + mask parity, empty ruleset, invalid-geometry reason parity, antimeridian wrap, and a high-latitude pre-filter regression.
- **Property tests** (core/tests/proptest.rs): `within_distance_never_drops_a_within_rule` — the conservative pre-filter (incl. the ±180 wrap complement) never drops a within-N rule, oracled against geo's haversine primitives, with repeat-evaluation and RStar/LinearScan parity.
- **Temporal** (core/tests/temporal.rs, 13 tests): window admission, end-exclusive boundary, midnight wrap, empty window, daysOfWeek=0, missing/non-Int fields, `$and`/`$or`/`$nor` composition, `at` required/malformed/unused semantics, malformed `$activeAt` clause, resolution admission, malformed-instant no-panic.
- `cargo test --workspace` and `cargo clippy --workspace --all-targets` are green.